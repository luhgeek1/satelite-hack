[pytest]
addopts = -ra --strict-markers
asyncio_mode = auto
testpaths = tests
pythonpath = src
markers =
    unit: pure engine and helper tests, no infrastructure needed
    integration: tests that need the app with postgres and redis
